import { sinon, expect, assert, faker, diff } from 'test/helpers/index'

describe('test', () => {
  it('it works', (done) => {
    expect(true).to.be.a('boolean')
    done()
  })

  it('it works again', (done) => {
    expect(1).to.be.a('number')
    done()
  })
  it('it works again1', (done) => {
    expect(1).to.be.a('number')
    done()
  })
})
